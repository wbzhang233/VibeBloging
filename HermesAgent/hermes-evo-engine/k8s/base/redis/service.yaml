apiVersion: v1
kind: Service
metadata:
  name: redis-svc
  namespace: hermes-evo
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  type: ClusterIP
