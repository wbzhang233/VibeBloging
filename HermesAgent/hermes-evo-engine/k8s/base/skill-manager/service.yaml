apiVersion: v1
kind: Service
metadata:
  name: skill-manager-svc
  namespace: hermes-evo
spec:
  selector:
    app: skill-manager
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
  type: ClusterIP
