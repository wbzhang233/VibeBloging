apiVersion: v1
kind: Service
metadata:
  name: review-agent-svc
  namespace: hermes-evo
spec:
  selector:
    app: review-agent
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
  type: ClusterIP
