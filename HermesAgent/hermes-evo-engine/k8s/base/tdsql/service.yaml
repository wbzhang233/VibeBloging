apiVersion: v1
kind: Service
metadata:
  name: tdsql-svc
  namespace: hermes-evo
spec:
  selector:
    app: tdsql
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  type: ClusterIP
