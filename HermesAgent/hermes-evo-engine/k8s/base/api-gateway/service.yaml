apiVersion: v1
kind: Service
metadata:
  name: api-gateway-svc
  namespace: hermes-evo
spec:
  selector:
    app: api-gateway
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
